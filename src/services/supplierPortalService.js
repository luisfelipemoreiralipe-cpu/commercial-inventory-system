import axios from 'axios';import api from './api';
const portalApi=axios.create({baseURL:(process.env.REACT_APP_API_URL||'http://localhost:3333')+'/api/supplier-portal',timeout:10000,headers:{'Content-Type':'application/json'}});
portalApi.interceptors.request.use(config=>{const token=localStorage.getItem('supplierPortalToken');if(token)config.headers.Authorization=`Bearer ${token}`;return config;});
portalApi.interceptors.response.use(r=>r.data?.data??r.data,error=>{if(error.response?.status===401){localStorage.removeItem('supplierPortalToken');if(window.location.pathname!=='/supplier-portal/login')window.location.href='/supplier-portal/login';}return Promise.reject(new Error(error.response?.data?.message||'Erro de comunicação.'));});
export const portal={login:data=>portalApi.post('/auth/login',data),requestReset:email=>portalApi.post('/auth/request-reset',{email}),reset:data=>portalApi.post('/auth/reset',data),me:()=>portalApi.get('/me'),catalog:()=>portalApi.get('/catalog'),history:()=>portalApi.get('/history'),submit:data=>portalApi.post('/price-updates',data)};
export const portalAdmin={list:supplierId=>api.get(`/supplier-portal/admin/suppliers/${supplierId}/users`),create:(supplierId,data)=>api.post(`/supplier-portal/admin/suppliers/${supplierId}/users`,data),revoke:id=>api.post(`/supplier-portal/admin/users/${id}/revoke`),resetToken:id=>api.post(`/supplier-portal/admin/users/${id}/reset-token`)};
