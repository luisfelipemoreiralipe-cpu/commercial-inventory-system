import React,{useState}from'react';import styled from'styled-components';import{portal}from'../services/supplierPortalService';
const Page=styled.div`min-height:100vh;display:grid;place-items:center;padding:20px;`;const Form=styled.form`width:min(440px,100%);display:flex;flex-direction:column;gap:12px;padding:28px;background:${({theme})=>theme.colors.bgCard};border-radius:16px;color:${({theme})=>theme.colors.textPrimary};input,button{padding:12px;border-radius:8px;}input{background:${({theme})=>theme.colors.bgInput};border:1px solid ${({theme})=>theme.colors.border};color:inherit;}button{background:${({theme})=>theme.colors.primary};color:white;}`;
export default function SupplierPortalRecovery(){const[email,setEmail]=useState(''),[token,setToken]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');const request=async e=>{e.preventDefault();const r=await portal.requestReset(email);setMessage(r.message);};const reset=async()=>{const r=await portal.reset({token,password});setMessage(r.message);};return<Page><Form onSubmit={request}><h1>Recuperar acesso</h1><input type="email" required placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><button>Solicitar recuperação</button><hr/><p>Se recebeu um token do administrador:</p><input placeholder="Token" value={token} onChange={e=>setToken(e.target.value)}/><input type="password" placeholder="Nova senha forte" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={reset}>Alterar senha</button>{message&&<p>{message}</p>}</Form></Page>}
