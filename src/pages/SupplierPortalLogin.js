import React,{useState}from'react';import styled from'styled-components';import{useNavigate,Link}from'react-router-dom';import{portal}from'../services/supplierPortalService';
const Page=styled.div`min-height:100vh;display:grid;place-items:center;padding:20px;background:${({theme})=>theme.colors.bg};`;const Card=styled.form`width:min(420px,100%);padding:28px;background:${({theme})=>theme.colors.bgCard};border:1px solid ${({theme})=>theme.colors.border};border-radius:${({theme})=>theme.radii.xl};display:flex;flex-direction:column;gap:14px;h1{color:${({theme})=>theme.colors.textPrimary};}p{color:${({theme})=>theme.colors.textSecondary};}a{color:${({theme})=>theme.colors.info};}`;const Input=styled.input`padding:12px;border:1px solid ${({theme})=>theme.colors.border};border-radius:8px;background:${({theme})=>theme.colors.bgInput};color:${({theme})=>theme.colors.textPrimary};`;const Button=styled.button`padding:12px;border-radius:8px;background:${({theme})=>theme.colors.primary};color:white;font-weight:700;`;
export default function SupplierPortalLogin(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const nav=useNavigate();const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const result=await portal.login({email,password});localStorage.setItem('supplierPortalToken',result.token);nav('/supplier-portal');}catch(err){setError(err.message);}finally{setLoading(false);}};return<Page><Card onSubmit={submit}><h1>Portal do Fornecedor</h1><p>Acesse somente seu catálogo e seus envios de preço.</p><Input type="email" required placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><Input type="password" required placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p>{error}</p>}<Button disabled={loading}>{loading?'Entrando...':'Entrar'}</Button><Link to="/supplier-portal/recovery">Recuperar acesso</Link></Card></Page>}
